"""Helper scripts for the SCENIC+ microglia project.

Subpackages: ``pipeline`` (full workflow including ``post/`` embedding → progression → enrichment),
``utils`` (genome helpers, Leiden sweep CLI, barcodes).
"""
